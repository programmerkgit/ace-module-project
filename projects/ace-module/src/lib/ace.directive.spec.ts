import { AceDirective } from './ace.directive';

describe('AceDirective', () => {
  it('should create an instance', () => {
    const directive = new AceDirective();
    expect(directive).toBeTruthy();
  });
});
